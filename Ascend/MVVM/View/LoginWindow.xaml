<Window x:Class="Ascend.MVVM.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ascend.MVVM.View"
        mc:Ignorable="d"
        Height="650" 
        Width="500"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize">

    <!--Styles-->
    <Window.Resources>
        <!--Minimize Button Style-->
        <Style x:Key="MinimizeButtonStyle" 
               TargetType="Button">
            <Setter Property="Foreground" 
                    Value="#BEBEBE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--Hover-->
                            <Trigger Property="IsMouseOver" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" 
                                                             EndPoint="0,1">
                                            <GradientStop Color="#6366b8" 
                                                          Offset="0"/>
                                            <GradientStop Color="#42457a" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!--Klick-->
                            <Trigger Property="IsPressed" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" 
                                                             EndPoint="0,1">
                                            <GradientStop Color="#7174d1" 
                                                          Offset="0"/>
                                            <GradientStop Color="#3f4275" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--Maximize Button Style-->
        <Style x:Key="MaximizeButtonStyle" 
               TargetType="Button">
            <Setter Property="Foreground" 
                    Value="#BEBEBE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--Hover-->
                            <Trigger Property="IsMouseOver" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" 
                                                             EndPoint="0,1">
                                            <GradientStop Color="#6366b8" 
                                                          Offset="0"/>
                                            <GradientStop Color="#42457a" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!--Klick-->
                            <Trigger Property="IsPressed" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" 
                                                             EndPoint="0,1">
                                            <GradientStop Color="#7174d1" 
                                                          Offset="0"/>
                                            <GradientStop Color="#3f4275" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--Close Button Style-->
        <Style x:Key="CloseButtonStyle" 
               TargetType="Button">
            <Setter Property="Foreground" 
                    Value="#BEBEBE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--Hover-->
                            <Trigger Property="IsMouseOver" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" 
                                                             EndPoint="0,1">
                                            <GradientStop Color="#6366b8" 
                                                          Offset="0"/>
                                            <GradientStop Color="#42457a" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!--Klick-->
                            <Trigger Property="IsPressed" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" 
                                                             EndPoint="0,1">
                                            <GradientStop Color="#7174d1" 
                                                          Offset="0"/>
                                            <GradientStop Color="#3f4275" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedButton" 
               TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                CornerRadius="15" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--Hover-Effekt-->
                            <Trigger Property="IsMouseOver" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" 
                                                             EndPoint="0,1">
                                            <GradientStop Color="#AF46E8" 
                                                          Offset="0"/>
                                            <GradientStop Color="#7D1CE0" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!--Klick-Effekt-->
                            <Trigger Property="IsPressed" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" 
                                                             EndPoint="0,1">
                                            <GradientStop Color="#B86ADE" 
                                                          Offset="0"/>
                                            <GradientStop Color="#A11AFB" 
                                                          Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--Style für UserNameTextBox-->
        <Style x:Key="RoundedUserNameTextBoxStyle" 
               TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                CornerRadius="15" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="1.5" 
                                BorderBrush="Transparent" 
                                Padding="5">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background" 
                                        Value="#2d2e52"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="BorderBrush" 
                                        Value="#353761"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--Style für PasswordBox-->
        <Style x:Key="RoundedPasswordBoxStyle" 
               TargetType="PasswordBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" 
                                CornerRadius="15" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="1.5" 
                                BorderBrush="Transparent" 
                                Padding="5">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="Background" 
                                        Value="#2d2e52"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" 
                                     Value="True">
                                <Setter TargetName="border" 
                                        Property="BorderBrush" 
                                        Value="#353761"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20">

        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" 
                                 EndPoint="0,1">
                <GradientStop Color="#242543" 
                              Offset="0"/>
                <GradientStop Color="#19192e" 
                              Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <!--Fensterstruktur-->
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!--Titelleiste des Fensters-->
            <Border MouseDown="Border_MouseDown" 
                    CornerRadius="20,20,0,0"
                    Background="#1e1e38">

                <Grid HorizontalAlignment="Stretch">

                    <Label Content="Login Ascend" 
                           Foreground="White"
                           Margin="5,3,0,0"/>

                    <StackPanel HorizontalAlignment="Right" 
                                Orientation="Horizontal">

                        <Button Width="18"
                                Height="18"
                                Content="━" 
                                Cursor="Hand"
                                Style="{StaticResource MinimizeButtonStyle}" 
                                Click="ButtonMinimize_Click"
                                Margin="0,0,6,0">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" 
                                                     EndPoint="0,1">
                                    <GradientStop Color="#585aa3" 
                                                  Offset="0.0"/>
                                    <GradientStop Color="#272847" 
                                                  Offset="1.0"/>
                                </LinearGradientBrush>
                            </Button.Background>
                        </Button>

                        <Button Width="18"
                                Height="18"
                                Content="◻" 
                                Cursor="Hand"
                                Style="{StaticResource MaximizeButtonStyle}" 
                                Click="ButtonMaximize_Click"
                                Margin="0,0,6,0">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" 
                                                     EndPoint="0,1">
                                    <GradientStop Color="#585aa3" 
                                                  Offset="0.0"/>
                                    <GradientStop Color="#272847" 
                                                  Offset="1.0"/>
                                </LinearGradientBrush>
                            </Button.Background>
                        </Button>

                        <Button Width="18"
                                Height="18"
                                Content="🗙" 
                                Cursor="Hand"
                                Style="{StaticResource CloseButtonStyle}" 
                                Click="ButtonClose_Click"
                                Margin="0,0,9,0">
                            <Button.Background>
                                <LinearGradientBrush StartPoint="0,0" 
                                                     EndPoint="0,1">
                                    <GradientStop Color="#585aa3" 
                                                  Offset="0.0"/>
                                    <GradientStop Color="#272847" 
                                                  Offset="1.0"/>
                                </LinearGradientBrush>
                            </Button.Background>
                        </Button>

                    </StackPanel>
                </Grid>
            </Border>

            <!--Inhalt-->
            <Border Grid.Row="1" 
                    CornerRadius="0,0,20,20">

                <StackPanel VerticalAlignment="Center" 
                            HorizontalAlignment="Center">

                    <Image Source="/Icons/NewAscendLogo.png"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Width="300" 
                           Height="300"
                           RenderOptions.BitmapScalingMode="Fant"
                           Margin="0,0,0,0"/>

                    <Label Content="Username"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Foreground="#BEBEBE"
                           FontSize="25"/>

                    <TextBox x:Name="userNametxt"
                             Background="#272847"
                             Foreground="White"
                             BorderBrush="Transparent"
                             TextAlignment="Center"
                             FontSize="15"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Center"
                             Height="30"
                             Width="400"
                             Style="{StaticResource RoundedUserNameTextBoxStyle}"/>

                    <Label Content="Password"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Foreground="#BEBEBE"
                           FontSize="25"
                           Margin="0,5,0,0"/>

                    <PasswordBox x:Name="passwordtxt"
                                 Background="#272847"
                                 Foreground="White"
                                 BorderBrush="Transparent"
                                 HorizontalContentAlignment="Center"
                                 FontSize="15"
                                 VerticalAlignment="Center"
                                 HorizontalAlignment="Center"
                                 Height="30"
                                 Width="400"
                                 Style="{StaticResource RoundedPasswordBoxStyle}"/>

                    <Button x:Name="loginButton"
                            Click="LoginButton_Click"
                            Content="Log in"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            BorderThickness="1.5"
                            BorderBrush="#B86ADE"
                            Foreground="White"
                            FontSize="15"
                            Height="30"
                            Width="200"
                            Cursor="Hand"
                            Margin="0,25,0,0"
                            Style="{StaticResource RoundedButton}">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" 
                                                 EndPoint="0,1">
                                <GradientStop Color="#A11AFB" 
                                              Offset="0"/>
                                <GradientStop Color="#5415C3" 
                                              Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>

                    <Button x:Name="registerButton"
                            Click="RegisterButton_Click"
                            Content="Sign up"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            BorderThickness="1.5"
                            BorderBrush="#B86ADE"
                            Foreground="White"
                            FontSize="15"
                            Height="30"
                            Width="200"
                            Cursor="Hand"
                            Margin="0,20,0,0"
                            Style="{StaticResource RoundedButton}">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" 
                                                 EndPoint="0,1">
                                <GradientStop Color="#A11AFB" 
                                              Offset="0"/>
                                <GradientStop Color="#5415C3" 
                                              Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>

                </StackPanel>

            </Border>

        </Grid>

    </Border>

</Window>